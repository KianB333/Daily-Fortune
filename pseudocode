Primary goal: Create an interface that rolls three dice and returns the daily
  Suibian Temple fortune + what daily health goal that fortune connects to.

top section:
- "roll" button
- three side-to-side images of dice faces
- "core day reroll" button

bottom section:
- text body for the "fortune"
- text body for the health interpretation

backend:
  - roll button generates 3 random numbers 1-6, stores rolls as variable
    - assign 6 variables to each dice name (for clarity later)
  
  - set variables for each possible combination of dice (going to use a lot of "or" statements for "any red", "any blue", "two lightning", etc.)
  
  - Make roll button remove its own event listener and transfer it to reroll
  
  - return combination generated from the 3 numbers when roll is pressed
    - How to check for any of the three? Can't check against each roll. If I want to check for lightning and two reds, for example, it'll be looking for only roll1 as "lightning". But any of the rolls can be lightning if the other two are red. Just make a really long list of "or" statements and "and" statements? 
      - If ((roll1 || roll2 || roll3 == "lightning") && (roll1 || roll2 || roll3 == "sun" || "star" || "fire") && (roll1 || roll2 || roll3 == "sun" || "star" || "fire"))?? No way. (It's also crazy long for each outcome, but) it'll still read duplicates from the same die.
      - Maybe I need to use a "for" loop and tally how many rolls each dice type got, then make the outcomes check for dice tallies, which would mean that no dice roll would get duplicated.